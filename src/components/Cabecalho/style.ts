import styled from 'styled-components'

export const CabecalhoVaga = styled.header`
  background-color: ${(props) => props.theme.corSecundaria};
  color: ${(props) => props.theme.corPrincipal};
  text-align: center;
  padding: 24px 0;
`
export const CabecalhoTit = styled.h1`
  color: none;
`
